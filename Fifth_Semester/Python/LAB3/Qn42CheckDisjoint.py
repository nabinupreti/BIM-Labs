# Check if two sets have no common elements (disjoint)
s1 = {1, 2, 3}
s2 = {4, 5, 6}

# Check if s1 and s2 are disjoint
are_disjoint = s1.isdisjoint(s2)
print("Are s1 and s2 disjoint?", are_disjoint)
